<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Customers_without_contact_info</fullName>
    <active>true</active>
    <description>Customer must have at least an Email or Phone number which is needed for Communication Purpose.</description>
    <errorConditionFormula>AND(
  Is_Active__c = TRUE,
  ISBLANK(Phone__c),
  ISBLANK(Email__c)
)</errorConditionFormula>
    <errorMessage>Customer must have at least an Email or Phone number</errorMessage>
</ValidationRule>
